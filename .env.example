# Postgres connection string. For local dev with Docker:
#   docker run --name k12-pg -e POSTGRES_PASSWORD=postgres -p 5432:5432 -d postgres:16
# For Vercel, connect a Neon or Vercel Postgres integration and it will be populated automatically.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/postgres
